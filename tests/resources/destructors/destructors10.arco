import std;

A struct {
    y int;

    ~A() {
        std.print("Destroyed!", 10);
    }
}

a A;

fn main() {
    std.print("Begin ", 6);
    a.y = 44; // Setting a variable so the global gets generated.
}