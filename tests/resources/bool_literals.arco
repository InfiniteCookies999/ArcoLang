import std;

fn main() {
    true_bool bool = true;
    false_bool bool = false;
    if true_bool == true {
        std.print("ok1", 3);
    }
    if true_bool != false {
        std.print("ok2", 3);
    }
    if false_bool == false {
        std.print("ok3", 3);
    }
    if false_bool != true {
        std.print("ok4", 3);
    }
}