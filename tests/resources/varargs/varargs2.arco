// -- Test to make sure multiple arguments can
//    be passed to varargs functions.
import std;

fn foo(args int...) {
    std.print(args.length);
    std.print(" ", 1);
    std.print(args[0]);
    std.print(" ", 1);
    std.print(args[1]);
}

fn main() {
    foo(44, 122);
}