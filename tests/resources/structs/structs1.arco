// -- Test to make sure that struct declaration and
//    field accessing works.
import std;

Data struct {
    a int;
    b int;
}

fn main() {
    data Data;
    data.a = 55;
    data.b = 44;

    std.print(data.a);
    std.print(" ", 1);
    std.print(data.b);
}