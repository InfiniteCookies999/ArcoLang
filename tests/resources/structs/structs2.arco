// -- Test to make sure that struct intializers and
//    field accessing works.

Data struct {
	a int;
	b int;
}

fn main() {
	data Data = Data{
		55, 44
	};
	print(data.a);
	print(" ", 1);
	print(data.b);
}