// -- Test to make sure that struct intializers and
//    field accessing works.
import std;

Data struct {
    a int;
    b int;
}

fn main() {
    data Data = Data{
        55, 44
    };
    std.print(data.a);
    std.print(" ", 1);
    std.print(data.b);
}