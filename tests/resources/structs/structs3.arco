// -- Test to make sure that struct intializers work
//    with non-foldable arguments.
import std;

Data struct {
    a int;
    b int;
}

fn main() {
    c int = 55;
    data Data = Data{
        c, 44
    };
    std.print(data.a);
    std.print(" ", 1);
    std.print(data.b);
}