import std;

Message enum {
    FIRST  :: "Programming is fun";
    SECOND :: "cool message";
}

fn main() {
    first_message Message = Message.FIRST;
    std.print(first_message, 18);
    std.print(" ", 1);
    second_message Message = Message.SECOND;
    std.print(second_message, 12);
}