// -- Test to make sure that calling member functions
//    works.
import std;

A struct {
    a int;

    fn setv(v int) {
        a = v;
    }

    fn getv() int {
        return a;
    }
}

fn main() {
    a A;
    a.setv(1234);
    std.print(a.getv());
}