// -- Test to make sure that default constructor is called
//    when a variable is declared with a type that has a default
//    constructor.
import std;

MyStruct struct {
    a int;
    MyStruct() {
        a = 55;
    }
}

fn main() {
    s MyStruct;
    std.print(s.a);
}