// -- Test to make sure that foldable
//    array initialization works for
//    global const pointers.
import std;

arr const int* = [ 4, 6, 3 ];

fn main() {
    std.print(arr[0]);
    std.print(" ", 1);
    std.print(arr[1]);
    std.print(" ", 1);
    std.print(arr[2]);
}
