// -- Test to make sure that arrays
//    can contain strings as elements.
import std;

fn main() {
    a cstr[3] = [ "pizza", "is", "good" ];
    std.print(a[0], 5);
    std.print(" ", 1);
    std.print(a[1], 2);
    std.print(" ", 1);
    std.print(a[2], 4);
}