// -- Test to make sure that non-foldable arrays
//    can be declared and assigned directly to pointers.
import std;

fn main() {
    x0 int = 235;
    vals int* = [ x0, 2, 63 ];
    std.print(vals[0]);
    std.print(" ", 1);
    std.print(vals[1]);
    std.print(" ", 1);
    std.print(vals[2]);
}