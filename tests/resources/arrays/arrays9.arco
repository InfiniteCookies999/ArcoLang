// -- Test to make sure that the elements
//    of the array cast to the destination
//    type.
import std;

fn main() {
    a int16[5] = [int16][ 24, 1, 523 ];
    loop i int = 0; i < 5; i = i + 1 {
        std.print(cast(int) a[i]);
        std.print(" ", 1);
    }
}