// -- Test to make sure that when the
//    elements of an array are of different
//    types that they cast correctly.
import std;

fn main() {
    a int[5] = [ 14, 'c', 55, 1, 'y' ];
    loop i int = 0; i < 5; i = i + 1 {
        std.print(a[i]);
        std.print(" ", 1);
    }
}