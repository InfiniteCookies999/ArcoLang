// -- Test to make sure that arrays can be declared
//    and assigned directly to const pointers.
import std;

fn main() {
    vals const int* = [ 235, 2, 63 ];
    std.print(vals[0]);
    std.print(" ", 1);
    std.print(vals[1]);
    std.print(" ", 1);
    std.print(vals[2]);
}