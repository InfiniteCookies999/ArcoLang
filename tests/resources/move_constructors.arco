import std;

A struct {
    A moveobj(a A*) {
        std.print("Called!", 7);
    }
}

fn foo(b A) {
}

fn main() {
    std.print("Begin ", 6);
    a A = A{};
    std.print("a created ", 10);
    b A = moveobj(a);
    foo(moveobj(b));
}
